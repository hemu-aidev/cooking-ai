// js/config.js
//
// Points the frontend at the Application Layer's API.
//
// - Local dev (app layer running via `python run.py` on its default port):
//     const API_BASE_URL = 'http://localhost:5000';
// - If this frontend is served BY the Flask app itself (same origin),
//   set this to an empty string instead:
//     const API_BASE_URL = '';
const API_BASE_URL = 'http://localhost:5000';
